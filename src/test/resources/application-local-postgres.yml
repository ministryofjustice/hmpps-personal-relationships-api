spring:

  datasource:
    url: 'jdbc:postgresql://localhost:5773/personal-relationships-db'
    username: contacts
    password: contacts
  flyway:
    locations: classpath:/migrations/common,classpath:/migrations/test
    placeholders:
      dpr_user: dpr_user
      dpr_password: dpr_password